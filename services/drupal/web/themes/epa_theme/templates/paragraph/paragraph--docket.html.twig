{#
/**
 * @file
 * Theme override to display a paragraph.
 */
#}

{% if content.field_link|field_value[0]['#url'] and content.field_number|field_value|render -%}
  <a href="{{ content.field_link|field_value[0]['#url'] }}">{{ content.field_number|field_value|render }}</a>
{%- endif %}
