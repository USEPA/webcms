{#
 /**
  * @file
  * Theme override to display primary and secondary local tasks.
  *
  * Available variables:
  * - primary: HTML list items representing primary tasks.
  * - secondary: HTML list items representing primary tasks.
  * - modifier_class: Variant classes to modify component.
  * - heading: Accessible heading.
  * - tabs: Primary or Secondary tabs.
  *
  */
#}

{% if primary %}
  {% include '@components/button-group/button-group.twig' with {
    'modifier_classes': 'button-group--base',
    'heading': 'Primary tasks',
    'buttons': primary
  } only %}
{% endif %}

{% if secondary %}
  {% include '@components/button-group/button-group.twig' with {
    'modifier_classes': 'button-group--unstyled',
    'heading': 'Secondary tasks',
    'tabs': secondary
  } only %}
{% endif %}
