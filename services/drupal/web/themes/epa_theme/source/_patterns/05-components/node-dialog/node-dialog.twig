{% set classes = [
  'node-dialog',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

<ul class="{{ classes }}" role="dialog">
	{% for item in nodeDialog %}
		<li class="node-dialog__item">
			<a 
				class="node-dialog__link {% if item.is_active %}is-active{% endif %}"
				{% if item.url %} 
		          href="{{ item.url }}" 
		        {% else %}
		          role="link"
		          aria-disabled="true"
		        {% endif %}  
			>
				{{ item.title }}
			</a>
		</li>
	{% endfor %}
</ul>
