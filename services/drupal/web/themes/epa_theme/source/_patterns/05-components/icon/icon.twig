{% set classes = [
  'icon',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

{% if is_demo %}
  {% set icons = [
    'arrow',
    'close',
    'warning',
    'angle-double-left',
    'angle-double-right',
    'angle-left',
    'angle-right',
    'chevron-circle-left',
    'chevron-circle-right',
    'facebook-square',
    'file',
    'flickr-square',
    'instagram-square',
    'rss',
    'twitter-square',
    'youtube-square',
   ] %}
  {% for icon in icons %}
    <p>
      <svg class="{{ classes }}"{% if is_hidden %} aria-hidden="true"{% endif %}>
        <use xlink:href="{{ image_path }}sprite.artifact.svg#{{ icon }}"></use>
      </svg>
      {{ icon }}
    </p>
  {% endfor %}
{%- else -%}
  {% spaceless %}
    <svg class="{{ classes }}"{% if is_hidden %} aria-hidden="true"{% endif %}>
      <use xlink:href="{{ image_path }}sprite.artifact.svg#{{ icon_name }}"></use>
    </svg>
  {% endspaceless %}
{%- endif -%}
