{% set classes = [
  'icon',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

{% if is_demo %}
  {% set icons = [
    'angle',
    'angle-double-left',
    'angle-double-right',
    'angle-left',
    'angle-right',
    'announcement',
    'file',
    'magnifying-glass',
    'rss',
    'star-circle',
    'xmark',
    'warning',
    'chevron-circle-left',
    'chevron-circle-right',
    'minus-circle',
    'plus-circle',
    'times-circle',
    'square',
    'check-square',
    'facebook-square',
    'flickr-square',
    'instagram-square',
    'former-twitter-square',
    'twitter-square',
    'twitter',
    'youtube-square',
   ] %}
  {% for icon in icons %}
    <p>
      <svg class="{{ classes }}"{% if is_hidden %} aria-hidden="true"{% endif %}{% if label %} role="img"{% endif %}>
        {% if label %}<title>{{ label }}</title>{% endif %}
        <use href="{{ epa_theme_image_path }}sprite.artifact.svg#{{ icon }}"></use>
      </svg>
      {{ icon }}
    </p>
  {% endfor %}
{%- else -%}
  {% apply spaceless %}
    <svg class="{{ classes }}"{% if is_hidden %} aria-hidden="true"{% endif %}{% if label %} role="img"{% endif %}>
      {% if label %}<title>{{ label }}</title>{% endif %}
      <use href="{{ epa_theme_image_path }}sprite.artifact.svg#{{ icon_name }}"></use>
    </svg>
  {% endapply %}
{%- endif -%}
