{% set classes = [
  'facet-list',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

<ul class="{{ classes }}">
  {% for item in list %}
    <li class="facet-list__item">{{ item.content }}</li>
  {% endfor %}
</ul>
