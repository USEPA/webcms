{% set classes = [
  'file--is-private',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

{% include '@components/file/file.twig' with {
  'modifier_classes': classes,
  'access': access,
} %}
