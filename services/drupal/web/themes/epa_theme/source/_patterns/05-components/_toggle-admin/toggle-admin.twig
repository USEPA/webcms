{{ attach_library('epa_theme/toggle_admin') }}

{% set classes = [
  'js-toggle-admin',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

{% include '@components/button/button.twig' with {
  'is_button_tag': true,
  'text': 'Hide Admin Info',
  'modifier_classes': classes,
} %}
