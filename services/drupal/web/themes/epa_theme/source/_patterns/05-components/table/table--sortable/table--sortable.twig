{% set classes = [
  'usa-table--sortable',
  modifier_classes ? modifier_classes,
]|join(' ')|trim %}

{% include "@uswds/table/table.twig" with {
  'modifier_classes': classes,
  'caption': caption,
  'header': header,
  'footer': footer,
  'rows': rows,
}%}
