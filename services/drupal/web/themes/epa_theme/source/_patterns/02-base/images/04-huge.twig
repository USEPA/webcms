<img {% if modifier_classes %}class="{{ modifier_classes }}" {% endif %}src="{{ img.huge.src }}" alt="{{ img.huge.alt }}"{% if img.huge.height %} height="{{ img.huge.height }}"{% endif %}{% if img.huge.width %} width="{{ img.huge.width }}"{% endif %}>
